{
  "name": "template-vue2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server",
    "init": "cd client && cnpm install",
    "dev": "cd client && npm run dev",
    "build": "rm -rf public && cd client && npm run build"
  },
  "dependencies": {
    "compression": "^1.6.2",
    "express": "~4.13.4"
  }
}
